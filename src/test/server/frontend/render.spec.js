//~ import {renderPage} from '../../../server/frontend/render';

describe('server frontend', () => {
  it('renders home page', (done) => {
    //~ renderPage({
      //~ appState: {},
      //~ config: {},
      //~ host: 'localhost',
      //~ location: '/',
      //~ res: {},
    //~ }).done(done);
    done();

  });
});
